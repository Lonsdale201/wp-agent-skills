interface:
  display_name: "Better Route Atomic Idempotency"
  short_description: "Prevent concurrent duplicate write execution."
  default_prompt: "Use $br-atomic-idempotency for concurrent write safety and review namespace/URL replay scope before migration."
