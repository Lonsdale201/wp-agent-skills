interface:
  display_name: "Better Route Auth Middleware"
  short_description: "Pick and configure Better Route 1.1 authentication."
  default_prompt: "Use Better Route 1.1 authentication middleware and shared AuthContext for a protected REST endpoint."
