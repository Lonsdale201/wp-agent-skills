interface:
  display_name: "Better Route CORS Public Client"
  short_description: "Configure Better Route 1.1 CORS and preflight safely."
  default_prompt: "Configure Better Route 1.1 CORS bridging and an explicit middleware-protected OPTIONS route."
