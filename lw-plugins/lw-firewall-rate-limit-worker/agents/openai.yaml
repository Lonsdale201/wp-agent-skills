interface:
  display_name: "LW Firewall Rate Limit Worker"
  short_description: "Audit MU-worker detection, limits, and bans"
  default_prompt: "Use $lw-firewall-rate-limit-worker to audit how LW Firewall detects, rate-limits, buckets, and bans traffic to my custom endpoint."
